--- origsrc/file-4.20/configure.in	2007-03-01 16:11:12.000000000 -0600
+++ src/file-4.20/configure.in	2007-05-13 15:51:18.453125000 -0500
@@ -50,6 +50,21 @@
 AC_SUBST(fsect)
 AM_CONDITIONAL(FSECT5, test x$fsect = x5)
 
+AC_ARG_ENABLE(python,
+[  --enable-python      enable Python magic bindings],
+[if test "${enableval}" = yes; then
+  AM_PATH_PYTHON
+  with_python=yes
+else
+  AC_MSG_RESULT(no)
+  with_python=no
+fi], [
+  # disable by default
+  AC_MSG_RESULT(no)
+  with_python=no
+])
+AM_CONDITIONAL(WITH_PYTHON, test x$with_python = xyes)
+
 dnl Checks for programs.
 AC_PROG_CC
 AC_PROG_INSTALL
--- origsrc/file-4.20/python/Makefile.am	2004-11-24 13:19:16.000000000 -0600
+++ src/file-4.20/python/Makefile.am	2007-05-13 16:00:48.921875000 -0500
@@ -1,2 +1,15 @@
+if WITH_PYTHON
+
+PYTHON_CFLAGS = -I$(prefix)/include/python$(PYTHON_VERSION)
+PYTHON_LIBS = -L$(exec_prefix)/lib/python$(PYTHON_VERSION)/config -lpython$(PYTHON_VERSION)
+
+pyexec_LTLIBRARIES = magic.la
+
+magic_la_SOURCES = py_magic.c py_magic.h
+magic_la_CFLAGS = -I$(top_srcdir)/src $(PYTHON_CFLAGS)
+magic_la_LDFLAGS = -module -avoid-version -no-undefined
+magic_la_LIBADD = $(top_builddir)/src/libmagic.la $(PYTHON_LIBS)
+
+endif
 
 EXTRA_DIST = README example.py py_magic.c py_magic.h setup.py
